[project]
name = "omc-agent"
readme = "README.md"
version = "0.0.1"

dependencies = ["fast-agent-mcp>=0.2.21"]

requires-python = ">=3.13"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
